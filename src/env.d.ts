// This file tells TypeScript about Astro's built-in globals and module types
// (e.g. `import.meta.env`, image imports, content collections).
// Astro generates it automatically on first run — you don't need to edit it.
/// <reference types="astro/client" />
