// Shared unit constants for p5.book

const UNITS = ["in", "cm", "mm", "px", "pt"];

const MM_PER_UNIT = {
  in: 25.4,
  cm: 10,
  mm: 1,
  pt: 25.4 / 72,
  px: 25.4 / 96,
};

export { UNITS, MM_PER_UNIT };
